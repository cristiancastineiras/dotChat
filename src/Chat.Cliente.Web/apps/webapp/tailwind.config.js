// ==============================================================================
// TAILWIND CSS v4
// ==============================================================================
// A partir de la versión 4 la configuración vive en CSS, no aquí. El complemento
// de Vite lee `src/index.css` y toma de él el tema (`@theme`), las rutas que hay
// que escanear (`@source`) y las utilidades propias (`@utility`).
//
// Este archivo se conserva vacío a propósito: un `theme` declarado aquí no se
// aplicaría —Tailwind v4 solo lo lee si se le apunta con `@config`— y tener dos
// definiciones del tema, una de ellas muerta, es la forma más rápida de acabar
// depurando un color que nunca llegó a existir.
//
// Los tokens de dotChat están en: apps/webapp/src/index.css
// ==============================================================================

/** @type {import('tailwindcss').Config} */
export default {}
